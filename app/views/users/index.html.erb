<% provide(:title, 'All Users') %>
<h1>All Users</h1>

<%= will_paginate(renderer: WillPaginate::ActionView::Bootstrap4LinkRenderer,
                  class: 'text-center') %>

<ul class="users">
  <%= render @users %>
</ul>

<%= will_paginate(renderer: WillPaginate::ActionView::Bootstrap4LinkRenderer,
                  class: 'text-center') %>
